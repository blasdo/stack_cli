name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: fedora-lastest

    steps:
    - uses: actions/checkout@v3
    - name: make
